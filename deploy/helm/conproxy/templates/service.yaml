apiVersion: v1
kind: Service
metadata:
  name: {{ include "conproxy.fullname" . }}
  labels:
    {{- include "conproxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: grpc
      port: {{ .Values.service.grpc.port }}
      targetPort: grpc
      {{- if and (eq .Values.service.type "NodePort") .Values.service.grpc.nodePort }}
      nodePort: {{ .Values.service.grpc.nodePort }}
      {{- end }}
    - name: http
      port: {{ .Values.service.http.port }}
      targetPort: http
      {{- if and (eq .Values.service.type "NodePort") .Values.service.http.nodePort }}
      nodePort: {{ .Values.service.http.nodePort }}
      {{- end }}
  selector:
    {{- include "conproxy.selectorLabels" . | nindent 4 }}
